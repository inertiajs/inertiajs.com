import dedent from 'dedent-js'
import Layout from '../components/Layout'
import Notice from '../components/Notice'
import TabbedCodeExamples from '../components/TabbedCodeExamples'

export default Layout
export const meta = {
  title: 'Analytics',
}

# Analytics

It's possible to add analytics to your InertiaJS app by declaring it in the VueJS mounted() callback of your layout file.

<TabbedCodeExamples
  examples={[
    {
      name: 'Vue.js',
      language: 'js',
      code: dedent`
        <template>
        // your layout
        </template>
        <script>\n
        export default {
          props: {
            url: String,
          },
          mounted() {
            // for example, Google Analytics via Google Tag Manager
            var dataLayer = window.dataLayer || [];
            dataLayer.push({
                'event': 'pageView',
                'virtualUrl': this.$inertia.page.url;,
            });
          }
        }
        </script>
      `,
    },
  ]}
/>

