import Layout from '../components/Layout'

export default Layout
export const meta = { title: 'Installation' }

# Installation

To use Inertia you need both a server-side adapter (ie. Laravel) as well as a client-side adapter (ie. Vue.js). Be sure to also follow the installation instructions below for the frameworks you use.
